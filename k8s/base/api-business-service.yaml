apiVersion: v1
kind: Service
metadata:
  name: rag-api-business
  labels:
    app.kubernetes.io/name: rag-api-business
    app.kubernetes.io/part-of: rag-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-api-business
  ports:
    - name: http
      port: 3000
      targetPort: http
