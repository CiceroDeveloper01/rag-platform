apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-api-web
  labels:
    app.kubernetes.io/name: rag-api-web
    app.kubernetes.io/part-of: rag-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: rag-api-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rag-api-web
        app.kubernetes.io/part-of: rag-platform
    spec:
      containers:
        - name: api-web
          image: ghcr.io/your-org/rag-platform-api-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: APP_NAME
              value: rag-platform-api-web
            - name: PORT
              value: "3000"
          envFrom:
            - configMapRef:
                name: rag-platform-config
            - secretRef:
                name: rag-platform-secrets
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "600m"
              memory: "768Mi"
