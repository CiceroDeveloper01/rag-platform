apiVersion: v1
kind: Service
metadata:
  name: rag-orchestrator
  labels:
    app.kubernetes.io/name: rag-orchestrator
    app.kubernetes.io/part-of: rag-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-orchestrator
  ports:
    - name: http
      port: 3001
      targetPort: http
