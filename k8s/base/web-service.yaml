apiVersion: v1
kind: Service
metadata:
  name: rag-web
  labels:
    app.kubernetes.io/name: rag-web
    app.kubernetes.io/part-of: rag-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-web
  ports:
    - name: http
      port: 80
      targetPort: http
